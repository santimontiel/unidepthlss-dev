# UniDepth-LSS
<p align="center">
  <strong>Accepted at the DriveX Workshop, ECCV 2026</strong>
</p>

UniDepth-LSS is a camera-only Bird’s-Eye-View (BEV) vehicle segmentation framework that integrates frozen metric depth priors from **UniDepthV2** into the geometric projection pipeline of **Lift-Splat-Shoot (LSS)**.

Unlike conventional LSS, which learns a discrete depth distribution using only BEV supervision, UniDepth-LSS uses metric depth predicted by a pretrained depth foundation model to guide the lifting of image features into 3D space. Features from the six surrounding cameras are projected into a shared ego-vehicle coordinate frame, aggregated into a BEV representation, refined using a lightweight transformer, and processed by a segmentation head.

The UniDepthV2 backbone remains frozen during training, while only the feature-projection, BEV-refinement, and segmentation components are trained. The method is evaluated on camera-only BEV vehicle segmentation using the nuScenes dataset.

## Model Architecture

The following figure presents the overall UniDepth-LSS architecture. The frozen UniDepthV2 backbone extracts metric depth and visual features from the multi-camera RGB input. The predicted metric depth guides feature lifting, ego-frame projection, voxel aggregation, BEV refinement, and vehicle segmentation.

<p align="center">
  <img src="Images/BEV_BlockDiagarm.jpg"
       alt="Block diagram of the UniDepth-LSS architecture"
       width="900">
</p>

<p align="center">
  <em>Overview of UniDepth-LSS using frozen metric depth priors for depth-guided BEV feature lifting.</em>
</p>

## Qualitative Results

The following figure presents qualitative BEV vehicle segmentation results on nuScenes. Each example includes the six surrounding RGB camera views, the corresponding ground-truth BEV vehicle map, and the prediction generated by UniDepth-LSS.

<p align="center">
  <img src="Images/Results_1.jpg"
       alt="Qualitative BEV vehicle segmentation results produced by UniDepth-LSS"
       width="900">
</p>

<p align="center">
  <em>Qualitative comparison between ground-truth and predicted BEV vehicle maps on nuScenes.</em>
</p>


##  Pre-trained Weights

Pre-trained weights for **UniDepth-LSS** are available here:

👉 [Download Pre-trained Weights (v1.0)](https://github.com/adeelmhr/UniDepth-LSS/releases/tag/v1.0)


## Acknowledgements

This work builds upon ideas and implementations from the following repositories:

- [Lift-Splat-Shoot](https://github.com/nv-tlabs/lift-splat-shoot), which provides the geometric framework for lifting multi-camera image features into 3D space and aggregating them into a shared BEV representation.
- [UniDepth](https://github.com/lpiccinelli-eth/UniDepth), which provides the pretrained UniDepthV2 metric depth foundation model used for depth and feature extraction.

We thank the authors of both projects for making their research and implementations publicly available.

## Citation

If you find this work useful, please cite our paper:

```bibtex
@inproceedings{hafeez2026unidepthlss,
  title     = {UniDepth-LSS: Depth Foundation Model Priors for Camera-Only BEV Perception},
  author    = {Hafeez, Muhammad Adeel and others},
  booktitle = {DriveX Workshop at the European Conference on Computer Vision},
  year      = {2026}
}
```
